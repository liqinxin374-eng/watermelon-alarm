# 定时闹钟 · 构建与出包指南

> 本目录是从 WorkBuddy 沙箱迁移出来的**源码级**工程（含 `src/`、`android/`、`ios/`、`scripts/`、配置与 `deliverables/` 文档）。
> 沙箱环境无法运行 `react-native init`（模板拷贝被拦截）且是 Windows（无法编译 iOS），所以这里只交付**源码 + 配置**，由你在自己的机器上补齐原生骨架并出包。
> 你本机的 `react-native init` 是正常的——只需按下面的「三步」即可拿到可安装的 `.apk` / `.ipa`。

---

## 0. 你最终会得到什么
- **Android**：`app-release.apk`（或调试 `app-debug.apk`），可在任意安卓手机安装。
- **iOS**：`.ipa`，通过 **EAS 云端构建**（在 Windows 上发起，云端 Mac 编译）或本地 Mac + Xcode 得到。
- ⚠️ iOS 限制（方案1）：铃声须 ≤30s 且打包进 Bundle；**App 被手动杀死后不响**——这是 iOS 系统级限制（详见 `deliverables/software-company/alarm-clock/framework.md` §5）。

---

## 1. 在你本机补齐完整 RN 原生工程（一次性的事）

在**任意临时目录**（非本目录）生成标准 RN 0.74 工程：

```bash
# 需要 Node 18/20/22 + JDK 17+（你机器已有 JDK 21 可用）
npx react-native@0.74.5 init AlarmClock --directory %TEMP%/RNScaffold
cd %TEMP%/RNScaffold
```

> 注意：项目名用 `AlarmClock`，这样原生包名会是 `com.alarmclock`，与本目录里 `android/.../com/alarmclock/alarm/*.kt` 的包声明一致。

---

## 2. 把本目录的源码「覆盖」进脚手架（文件映射表）

| 本目录文件 | 复制到脚手架的位置 | 说明 |
|---|---|---|
| `src/**` | `RNScaffold/src/**` | 直接用我们的引擎/桥接/UI 覆盖 |
| `App.tsx` | `RNScaffold/App.tsx` | 覆盖示例页 |
| `index.js` `app.json` `metro.config.js` `react-native.config.js` `tsconfig.json` | `RNScaffold/` 根 | 同名覆盖；`app.json` 已设 `displayName:"定时闹钟"` |
| `README.md` | `RNScaffold/README.md` | 覆盖 |
| `eas.json` `fastlane/` `.github/` `scripts/` | `RNScaffold/` 根 | 直接复制 |
| `android/app/src/main/java/com/alarmclock/alarm/*.kt` `AlarmPackage.java` | `RNScaffold/android/app/src/main/java/com/alarmclock/alarm/` | 我们的原生闹钟模块（Kotlin）+ 注册包（Java） |
| `android/app/src/main/AndroidManifest.xml.example` | → 合并进 `RNScaffold/android/app/src/main/AndroidManifest.xml` | 见步骤 3 |
| `android/generate-keystore.bat` | `RNScaffold/android/` | 发布签名生成脚本 |
| `ios/AlarmBridge/*` | `RNScaffold/ios/AlarmBridge/` | 我们的 iOS 原生模块（Swift/ObjC） |

### package.json 处理（重要）
**不要直接覆盖**脚手架的 `package.json`（它含 `react`/`react-native`/`@react-native-community/cli` 等必需依赖）。
请保留脚手架的 `package.json`，仅并入我们这份里的两项：
- `scripts` 中加 `"test": "tsx __tests__/recurrence.test.ts"`
- `devDependencies` 加 `tsx`、`typescript`
（我们的 `package.json` 仅含这两项，缺 RN 运行时依赖，直接覆盖会导致 `npm install` 后无法运行。）

---

## 3. 必须手工合并的两处原生配置

### 3.1 Android：合并 AndroidManifest.xml
打开 `RNScaffold/android/app/src/main/AndroidManifest.xml`，把 `android/app/src/main/AndroidManifest.xml.example` 里的以下内容并入（注意放到 `<application>` 内/外的正确位置）：
- `<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />`
- `<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />`（Android 13+）
- `<receiver android:name=".alarm.AlarmReceiver" .../>`
- `<service android:name=".alarm.AlarmService" android:exported="false" />`
- `<activity android:name=".alarm.AlarmRingActivity" android:exported="false" android:theme="@style/Theme.AppCompat.NoActionBar" />`

并在 `MainApplication.java`（或 `.kt`）的 `getPackages()` 中加 `new AlarmPackage()`。

### 3.2 iOS：注册通知中心
在 `RNScaffold/ios/AlarmClock/AppDelegate.swift`（或 `.mm`）的 `didFinishLaunchingWithOptions` 里加：
```swift
UNUserNotificationCenter.current().delegate = self
UNUserNotificationCenter.current().requestAuthorization(options: [.sound, .alert]) { _, _ in }
```
把 `ios/AlarmBridge/` 四个文件加入 Xcode 工程同一 target，并在 Build Settings 设 `SWIFT_OBJC_BRIDGING_HEADER = AlarmBridge/AlarmBridge-Bridging-Header.h`。
铃声 `default.wav`（≤30s）通过 `python3 scripts/gen-ringtone.py` 生成后拖入 Bundle。

---

## 4. 安装依赖
```bash
cd RNScaffold
npm install        # 拉取 react-native 及全部依赖（含本目录的 tsx/typescript）
npm test           # 验证周期引擎：应 8/8 通过（时区/夏令时已覆盖）
```

---

## 5. Android 出包

### 调试包（最快，免签名，可直接装）
```bash
cd RNScaffold/android
./gradlew assembleDebug
# 产物：app/build/outputs/apk/debug/app-debug.apk
adb install -r app/build/outputs/apk/debug/app-debug.apk
```
> 首次构建会下载 Gradle 与依赖，耗时较长。SDK 路径：确保在 `local.properties` 设 `sdk.dir=你的SDK路径`（你机器为 `E:\C盘瘦身搬家文件路径\Android\Sdk`，若 Gradle 报非 ASCII 路径错误，建议用短路径或把 SDK 移到纯英文路径）。

### 发布包（含签名）
```bash
cd RNScaffold/android
generate-keystore.bat        # 生成 release.keystore（按提示填密码/别名）
# 在 gradle.properties 与 app/build.gradle 配置 signingConfigs（见 README「构建与打包」）
./gradlew assembleRelease
# 产物：app/build/outputs/apk/release/app-release.apk（或 .aab 上架用）
```
装到手机后：**引导加入电池优化白名单**，否则后台/锁屏可能被杀。

---

## 6. iOS 出包

### 方式 A · EAS 云端（推荐，无需 Mac，Windows 即可发起）
```bash
npm i -g eas-cli
eas login                 # 需 Expo 账号（免费）
eas build -p ios --profile production
# 构建在云端 Mac 完成，结束后给下载链接，拿到 .ipa 用爱思/Apple Configurator 装
```
> 若云端报 `ios/` 缺 xcodeproj：先在本机（或 Mac）执行一次 `npx react-native init` 生成原生工程再走 EAS（即本指南第 1–3 步已做的部分）。

### 方式 B · 本地 Mac + Xcode
```bash
cd RNScaffold/ios
pod install
open AlarmClock.xcworkspace
# 选真机/Generic iOS Device → Product → Archive → 导出 .ipa
```

---

## 7. 真机验证清单
1. `npm test` 引擎 8/8 通过。
2. 设一个「1 分钟后」的闹钟，验证三种状态响铃：
   - App 前台 ✅
   - App 退到后台 ✅
   - App 被手动杀死（Android 应仍响；**iOS 方案1 不响，属预期**）⚠️
3. 周期性：每天/每周/每月/每年各设一条，确认 `nextTrigger` 计算正确。

---

## 8. 常见问题
- **Q：`react-native init` 在你机器也失败？** 一般不会；若报错多为网络/Node 版本，换 Node 20 LTS 重试。
- **Q：引擎单测在哪？** 本目录 `__tests__/recurrence.test.ts`，已在沙箱验证 8/8。
- **Q：想让 iOS 被杀后也响？** 需 Critical Alert entitlement（苹果特殊授权，普通闹钟品类难过审），详见框架文档 §5。
- **Q：更多架构/原生细节？** 看 `deliverables/software-company/alarm-clock/framework.md`。
